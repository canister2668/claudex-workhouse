# Proton Drive 결과물 업로드 구현 계획 및 완료 기록

## 목표

완료된 로컬 작업의 결과물을 사용자가 명시적으로 선택·검토·확인한 경우에만 Proton Drive의 `/my-files/Claudex-Workhouse` 아래로 업로드한다. 프롬프트 문구를 자동 실행 신호로 해석하거나 공유 링크를 생성하지 않는다.

## 클로드 리뷰 반영 결정

- Proton 공식 CLI의 명령과 인증을 별도 어댑터로 격리하고 가짜 CLI로 회귀 테스트한다.
- 계정 비밀번호·토큰·세션은 Workhouse DB에 저장하지 않는다. CLI가 지원하는 OS 자격 증명 저장소만 사용한다.
- 전송 대상은 완료된 로컬 작업과 그 작업에 연결된 워크스페이스의 일반 파일로 제한한다.
- 모든 경로 구간에서 심볼릭 링크를 거부하고, 스테이징 복사 전후 파일 신원을 확인한 뒤 SHA-256을 계산한다.
- 업로드 직전 원격 경로·크기·SHA-256을 다시 보여 주고 별도의 확인을 받는다.
- 서버 재시작 또는 CLI 시간 초과로 전송 결과가 불명확하면 `delivery-uncertain`으로 기록하고 자동 재시도하지 않는다.
- 충돌 정책은 `skip`으로 고정하고 파일명에 SHA-256 앞 8자를 붙인다.

## 구현 범위

1. 글로벌 설정 UI
   - 계정 설정의 세 번째 `Proton Drive` 탭
   - CLI 설치·호환·로그인 상태, 로그인/로그아웃, 활성화, 원격 루트, 크기 제한, 전송 후 검증, 최근 기록
2. 업로드 UI
   - 완료 결과 카드의 명시적 `Proton에 업로드` 버튼
   - 워크스페이스 상대 경로 입력, 준비 단계, 전송 전 검토, 실행, 완료 경로 표시
3. 서버 및 보안
   - `shell:false` CLI 실행, 제한된 인자 구성, Proton 소유 로그인 URL만 노출
   - 경로 탈출·심볼릭 링크·비정규 파일·크기 초과·작업/워크스페이스 불일치 거부
   - 안정된 임시 복사본과 SHA-256 기반 원격 파일명
4. 영속성과 복구
   - DB 마이그레이션 21과 Python/Node SQLite 워커 동등 구현
   - 준비·실행·검증·완료·실패·취소·전송 불명 상태 수명주기
   - 호스트당 실행/검증 중 업로드 1건 제한과 재시작 조정
5. 검증
   - 설정·CLI 인자·로그인 URL 허용 목록·업로드·심볼릭 링크 거부 단위 테스트
   - DB 동시성/재시작 회귀 테스트와 UI 연결 테스트
   - 정적 검사, 서버/웹 빌드, 재시작 전 DB 스냅샷, 라이브 API/UI 확인

## 현재 검증 상태

2026-08-08 기준 공식 Proton Drive CLI 0.7.0을 NAS 호환 런타임과 `pass` 자격 증명 저장소로 실행하고 로그인·목록 조회·실제 파일 전송을 확인했다. 에렌샤 통합 모듈 1개와 얼헌 DLC 5개(총 약 992 MB)를 전송한 뒤 원격 `claimedSize`와 SHA-1을 로컬 파일과 대조해 모두 일치함을 확인했다.

CLI가 로컬 경로의 `*`, `?`, `[`, `{`를 Node glob 문법으로 해석하므로 어댑터에서 해당 문자를 이스케이프한다. 전송 후 검증은 `filesystem info --json`의 `activeRevision.claimedSize`와 `claimedDigests.sha1`을 준비된 파일과 비교한다. `PROTON_DRIVE_CREDENTIALS_STORE=unsafe_file`은 사용하지 않는다.
